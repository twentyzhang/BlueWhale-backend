# Lombok 全局配置
# 让 @RequiredArgsConstructor / @AllArgsConstructor 生成的构造器参数保留 Spring 的 @Qualifier，
# 以支持「同类型多 Bean + 构造器注入」按名消歧（如多个 TaskScheduler 中注入 paymentCallbackScheduler）。
config.stopBubbling = true
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
