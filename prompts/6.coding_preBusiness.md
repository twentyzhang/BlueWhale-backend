帮我创建统一的 API 响应封装类 Result`<T>`，
放在 com.twentyzhang.BlueWhale.common 包下。

要求：

- 包含字段：code（整数）、message（字符串）、data（泛型）
- 提供静态方法：success()、success(data)、fail(message)、fail(code, message)
- 成功 code 为 200，失败 code 根据场景区分（400参数错误、401未登录、403无权限、404不存在、500服务器错误）
- 使用 Lombok

---



帮我创建全局异常处理器，放在 com.twentyzhang.BlueWhale.exception 包下。

要求：

- 使用 @RestControllerAdvice
- 处理以下异常并返回对应的 Result 格式：
  - MethodArgumentNotValidException（Validation 校验失败 → 400）
  - 自定义 BusinessException（业务异常 → 按异常内的 code 返回）
  - RuntimeException（未知错误 → 500）
- 同时创建 BusinessException 自定义异常类，包含 code 和 message 字段
