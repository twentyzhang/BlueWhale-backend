> 注意，下面是几段是分开来多次对话，不是让claude code一次性生成全部

基于 docs/database-design.md，帮我生成所有 Entity 类。

要求：

- 包路径：com.twentyzhang.BlueWhale.entity
- 使用 MyBatis-Plus 注解（@TableName、@TableId、@TableField）
- 使用 Lombok（@Data、@Builder、@NoArgsConstructor、@AllArgsConstructor）
- 主键策略使用 @TableId(type = IdType.AUTO)
- 每个类包含基础字段：id、createTime、updateTime
- updateTime 和 createTime 使用 @TableField(fill = FieldFill.INSERT) / UPDATE 自动填充注解标注
- 只生成类结构和字段，不需要写业务方法

---



基于 docs/api-spec.md，帮我生成所有请求和响应的 DTO 类。

要求：

- 包路径：com.twentyzhang.BlueWhale.dto
- 请求类命名：XxxRequest
- 响应类命名：XxxResponse
- 使用 Lombok
- 请求类加上 Validation 注解

---



基于已生成的 Entity 类，帮我生成所有 Mapper 接口。

要求：

- 包路径：com.twentyzhang.BlueWhale.mapper
- 继承 BaseMapper<对应Entity>
- 只添加 docs/api-spec.md 中明确需要的自定义查询方法签名
- 在 mapper 接口上加 @Mapper 注解

---



基于 docs/api-spec.md，帮我生成所有 Service 接口和对应的 ServiceImpl 类。

要求：

- 接口包路径：com.twentyzhang.BlueWhale.service
- 实现类包路径：com.twentyzhang.BlueWhale.service.impl
- ServiceImpl 继承 ServiceImpl<对应Mapper, 对应Entity>
- 只生成方法签名，方法体全部抛出 UnsupportedOperationException
- 方法上加注释说明该方法的用途


---


基于 docs/api-spec.md 和已生成的 Service 接口，帮我生成所有 Controller 类。

要求：

- 包路径：com.twentyzhang.BlueWhale.controller
- 使用 @RestController、@RequestMapping
- 每个方法只调用对应 Service 方法并返回结果，不写具体逻辑
- 加上 @Valid 注解做参数校验
- 需要登录的接口加注释标注 // 需要鉴权

---

帮我创建 MyBatis-Plus 的自动填充处理器 MetaObjectHandler，
实现 createTime 和 updateTime 的自动填充，
放在 com.twentyzhang.BlueWhale.config 包下

---

帮我在 com.twentyzhang.BlueWhale.config 包下配置
MyBatis-Plus 的分页插件 MybatisPlusInterceptor，
数据库类型为 MySQL
