请完成以下两项技术债清理：

1. 将所有 Controller 中的 getCurrentUserId() / getCurrentUserRole()
   改为从 SecurityContext 中读取 AuthUser：
   AuthUser user = (AuthUser) SecurityContextHolder
   .getContext().getAuthentication().getPrincipal();
2. 将所有 Controller 的返回值从 ApiResponse`<T>` 全面替换为 Result`<T>`，
   替换完成后删除旧的 ApiResponse 类

请逐个 Controller 文件处理，处理完告诉我改动了哪些文件
