# AI 导购 Agent + 生产化护栏 — 实施进度与恢复点

> 配套计划：[2026-06-29-ai导购agent与生产化护栏.md](./2026-06-29-ai导购agent与生产化护栏.md)
> 设计：[../specs/2026-06-29-ai导购agent与生产化护栏-design.md](../specs/2026-06-29-ai导购agent与生产化护栏-design.md)
> 执行方式：子代理驱动开发（每任务 implementer + reviewer + 必要时 fix）
> 最后更新：2026-06-30（全部 12 任务完成；最终 whole-branch review 通过；2 个 Important 已修；待合并）

## 当前状态

- **分支**：`feat/ai-agent-guardrails`（未并入 main）。HEAD = `b73f231`。
- **进度：12/12 任务全部完成并评审 Approved。** 最终整支评审裁定 *Ready to merge — With fixes*，两个 Important 均已修复（embedding 客户端超时 `ae54f0d`；注册强制 CUSTOMER 防提权 `b73f231`）。全量测试 353 run / 0 失败 / 0 错误 / 1 跳过（Qdrant assumeTrue）。**特性完成，待合并决策。**

## 任务清单与状态

| # | 任务 | 状态 | 关键提交 |
| --- | --- | --- | --- |
| 1 | LLM 消息模型 + AgentChatClient 接口 | ✅ 已评审 | 850a768 |
| 2 | QwenAgentChatClient（qwen tools） | ✅ 已评审 | b9ada0b |
| 3 | Tool 接口 + AgentContext + ToolRegistry | ✅ 已评审 | 9460805 |
| 4 | 公开只读工具 ×4（search/detail/stock/coupons） | ✅ 已评审（含测试强化） | 298d5b0 + 4e415ce |
| 5 | 个人工具 ×2（my-orders/my-coupons，防越权） | ✅ 已评审（含越权测试强化） | 7e17a4f + 746da5c |
| 6 | AssistantAgentService（Agent 循环 + SSE） | ✅ 已评审（含断开短路+并发修复） | 4a010dd + 8a6e7ac + 163b6a3 |
| 7 | AssistantController + agent 配置 | ✅ 已评审（2 Minor 留最终评审） | 58cad8c |
| 8 | 端到端集成测试（桩 AgentChatClient） | ✅ 已评审（初评 Important 已修：补 products 断言） | edb844f + f585524 |
| 9 | Resilience4j 超时/熔断/重试 + 4 个新依赖 | ✅ 已评审（2 Minor 留最终评审） | 0ce7419 |
| 10 | 限流（Redis Lua 令牌桶 + 拦截器） | ✅ 已评审（1 Minor 留最终评审） | 1ef8bf6 |
| 11 | 监控（Micrometer 指标 + Actuator/Prometheus） | ✅ 已评审（1 Minor 留最终评审） | f42f6ae |
| 12 | 全量回归 + 文档同步 | ✅ 完成（回归中发现并修复 DashScope 集成测试超时 bug） | 23d50f0 + 08c03e9 + 8655e0a |

## 下一会话如何恢复

1. **特性已全部完成并通过最终整支评审**（2026-06-30）。剩余动作仅为合并决策（merge / PR），见 `finishing-a-development-branch`。
2. **合并后的可选 follow-up**（最终评审 deferred minors，均非阻塞）：详见 `.superpowers/sdd/progress.md` 末「最终评审 deferred minors」一节——toWireMessages content:""、断开时 rounds 指标失真、embed 空向量显式降级、max-attempts 注释、未来 @WebMvcTest mock 约定、残留测试注释。
3. **全程 SDD ledger**（最详细，含每任务评审/修复 + minor 滚动清单）：`.superpowers/sdd/progress.md`（git-ignored，本机留存）。

## 执行中已发现并处理的偏差（供最终评审参考）

- **计划修正**：`ProductService` 实际方法为 `getProductById(Long)`（非计划写的 `getProductDetail`），返回 `ProductDetailResponse`（含 stock/name/id）。仅 Task 4 涉及，已修正。
- **Task 6 提前创建** `config/AgentExecutorConfig.java`（`assistantStreamExecutor` bean，@Qualifier 注入所需）——原属 Task 7；Task 7 已据此缩减范围、未重复创建。
- **计划示例测试偏弱**：多处断言不够对抗（Task4 check_stock 恒真、Task5 coupons 越权仅 happy-path、Task6 normalFlow mock 有 bug）——均已在执行中强化为可证伪测试。
- **Task 6 并发 bug**（执行中引入并修复）：断开标志最初误用单例实例字段，会跨并发请求串扰；已改为每请求 `AtomicBoolean` 并加跨请求泄漏测试。

## 留给最终整支评审（whole-branch review）的 Minor

- Task 2：`toWireMessages` 对 assistant+tool_calls 消息置 `content:""`（OpenAI 规范应为 `null`/省略），切更严格 provider（DeepSeek）可能需改。
- Task 6：断开路径仍向已断开 emitter 发一条 error（无害，complete 仍恰好一次）；`stepLabel` 去掉了 emoji（Windows 编码顾虑，可在最终部署环境恢复）。
- 多个任务的测试文件首行曾残留 brief 代码块标签注释 `// Xxx.java`（早期 Task1/3）——最终评审顺手清理。
