# 南鲸商城 BlueWhale — 项目说明

> 一个专注国货品牌的在线购物平台后端，覆盖商品浏览、购物车、下单支付、库存管理、评论、优惠券与数据报表的全流程电商服务。
>
> 基于 Spring Boot 3 + MyBatis Plus + Spring Security(JWT) + Redis 构建，采用分层架构与无状态鉴权，业务逻辑全部完成并配有 199 个单元测试。

---

## 目录

- [一、项目简介](#一项目简介)
- [二、技术栈](#二技术栈)
- [三、角色与权限](#三角色与权限)
- [四、需求与功能模块](#四需求与功能模块)
- [五、系统架构](#五系统架构)
- [六、数据库设计](#六数据库设计)
- [七、核心实现要点](#七核心实现要点)
- [八、技术亮点](#八技术亮点)
- [九、快速开始](#九快速开始)
- [十、测试](#十测试)
- [十一、项目结构](#十一项目结构)
- [十二、文档导航](#十二文档导航)
- [十三、暂未实现与后续规划](#十三暂未实现与后续规划)

---

## 一、项目简介

南鲸商城（BlueWhale）是一个面向**消费者 / 门店店员 / 平台管理员**三类角色的多商店电商平台后端。它以 RESTful API 形式对外提供服务，前后端分离，前端可凭本仓库的 [前端对接文档](./frontend-handoff.md) 直接对接。

| 指标 | 数值 |
|---|---|
| 业务模块 | 10 个（用户 / 商店 / 分类 / 商品 / 购物车 / 订单 / 地址 / 评论 / 优惠券 / 报表） |
| REST 接口 | 46 个 |
| 数据表 | 12 张 |
| 主代码 Java 文件 | 129 个（含 15 Controller / 11 Service 接口 + 11 实现 / 12 Entity / 12 Mapper / 55 DTO） |
| 单元测试 | 199 个（18 个测试类，全部通过） |

---

## 二、技术栈

| 层次 | 选型 | 说明 |
|---|---|---|
| 语言 | Java 17 | |
| 框架 | Spring Boot 3.4.0 | |
| 构建 | Maven | |
| ORM | MyBatis Plus 3.5.9 | 含分页插件、逻辑删除、字段自动填充 |
| 安全 | Spring Security（无状态）+ JWT(jjwt 0.12.6) | 自定义过滤器解析 Token |
| 数据库 | MySQL 8.x | |
| 缓存 / 令牌存储 | Redis（Lettuce + commons-pool2 连接池） | 存储 refreshToken |
| 校验 | Jakarta Bean Validation | 请求体声明式校验 |
| 工具 | Lombok、Spring Boot DevTools | |
| 测试 | JUnit 5 + Mockito + spring-security-test | 纯 Mock 单元测试 |

---

## 三、角色与权限

| 角色 | 标识 | 能力 |
|---|---|---|
| 游客 Guest | （未登录） | 浏览商店 / 商品 / 分类 / 评论 / 可领券列表 |
| 顾客 Customer | `CUSTOMER` | 购物车、下单、支付、确认收货、取消、退款、评论、领券用券、地址管理 |
| 店员 Staff | `STAFF` | 管理**本店**商品 / 库存 / 订单（发货）/ 店铺优惠券，查看本店报表 |
| 管理员 Admin | `ADMIN` | 创建/维护商店、发布全局优惠券、删除分类、查看全局报表 |

**鉴权策略**：无状态 JWT。登录签发 accessToken（24h）与 refreshToken（7d，存 Redis）。请求头 `Authorization: Bearer <token>`，由 `JwtAuthenticationFilter` 解析为 `AuthUser` 写入 `SecurityContext`；具体的角色与归属校验由 Service 层的 `AuthUtil` 完成（路由放行与业务鉴权分离）。

---

## 四、需求与功能模块

> 完整需求见 [requirements.md](./requirements.md)，完整接口见 [api-spec.md](./api-spec.md)。

| 模块 | 核心功能 | 主要角色 |
|---|---|---|
| **用户** | 注册、登录、Token 刷新、改昵称、改密码、个人信息（手机号脱敏） | 全部 |
| **商店浏览** | 商店列表/详情、商品列表/详情/搜索（关键词+分类+价格区间）、分类树 | 游客 / 顾客 |
| **商品管理** | 新建/编辑/删除商品、库存出入库、嵌套分类管理 | Staff / Admin |
| **购物车** | 加购（同品累加）、改数量、删条目、结算 | 顾客 |
| **订单** | 下单（库存校验+优惠券核销）、支付、发货、确认收货、取消、退款 | 顾客 / Staff |
| **收货地址** | 多地址增删改、默认地址维护、下单选址 | 顾客 |
| **评论** | 查看评论（含楼层回复）、购买后评分评论、回复评论 | 游客 / 顾客 |
| **优惠券** | 发布券组（折扣/满减）、领取、下单核销、我的券 | 顾客 / Staff / Admin |
| **商店管理** | 创建商店并绑定店员、维护商店信息、查看所有商店 | Admin |
| **报表** | 门店订单报表、全局汇总报表（状态分布 / 每日营收 / 店铺分解） | Staff / Admin |

---

## 五、系统架构

经典分层 + 无状态安全过滤链：

```
                    HTTP 请求
                       │
            ┌──────────▼───────────┐
            │ JwtAuthenticationFilter│  解析 Bearer Token → AuthUser → SecurityContext
            └──────────┬───────────┘
                       │
            ┌──────────▼───────────┐
            │  SecurityFilterChain  │  /api/auth/** 与商品浏览类 GET 放行，其余需认证
            └──────────┬───────────┘
                       │
   Controller (@RestController)        路由、参数校验(@Valid)、统一包装 Result<T>
        │
   Service / ServiceImpl               业务逻辑 + AuthUtil 角色/归属鉴权 + @Transactional
        │
   Mapper (MyBatis Plus BaseMapper)    CRUD / 分页 / 逻辑删除；复杂聚合用自定义 SQL
        │
   MySQL  ＋  Redis(refreshToken)
```

**横切设施**：
- `Result<T>` 统一响应封装（code/message/data）。
- `GlobalExceptionHandler`（`@RestControllerAdvice`）：校验异常→400、`BusinessException`→自定义码、其余→500。
- `MybatisPlusConfig` 分页拦截器；逻辑删除（`deleted` 字段）全局生效。

---

## 六、数据库设计

> 建表脚本见 [V1__init_schema.sql](../src/main/resources/db/migration/V1__init_schema.sql)（由 Flyway 管理）。12 张表，统一 `deleted` 逻辑删除、`created_at/updated_at` 自动维护。

| 表 | 说明 | 设计要点 |
|---|---|---|
| `store` | 商店 | 信用代码唯一 |
| `user` | 用户 | 角色字段；Staff 外键关联 `store` |
| `product_category` | 商品分类 | 自关联 `parent_id` 支持嵌套树 |
| `product` | 商品 | 关联店铺与分类，价格/库存索引 |
| `user_address` | 收货地址 | `is_default` 标记默认地址 |
| `cart` / `cart_item` | 购物车 / 条目 | 一用户一车；车内同品唯一（数量叠加） |
| `coupon_group` / `coupon` | 券组 / 券实例 | 券组定义规则，券实例记录领取与使用状态 |
| `orders` | 订单 | **冗余收货地址快照**，避免改址影响历史订单 |
| `order_item` | 订单条目 | **冗余商品名/单价快照**，避免改价影响历史订单 |
| `review` | 评论 | `parent_id` 区分一级评价（含评分）与楼层回复 |

> `order` 为 MySQL 保留字，故表名用 `orders`。

---

## 七、核心实现要点

> 详细算法与边界处理见 [实现说明.md](./实现说明.md)。

- **下单两阶段设计**：先只读全量校验（购物车归属、地址、库存、优惠券状态与过期、金额计算），全部通过后再统一写库（扣库存→建订单→建条目快照→核销券→清购物车），整体 `@Transactional` 保证原子性。
- **金额计算**：`DISCOUNT` 按折扣率、`AMOUNT_OFF` 按满减门槛，`payableAmount = max(0.01, total − discount)`，统一 2 位 HALF_UP。
- **分类树构建**：一次全量查询 + 两次线性遍历（Map 建索引 → 按 parentId 挂载），O(n) 组装嵌套树，避免递归查库。
- **避免 N+1**：列表场景统一「二次批量查询」（收集本页外键 → `selectBatchIds` 一次查 → Map 回填），如商品的 categoryName、评论的昵称。
- **快照机制**：订单条目与收货地址在下单时落库快照，商品改价/改名、用户改址均不影响历史订单展示。
- **Token 刷新与轮换**：`POST /api/auth/refresh` 校验 Redis 中 refreshToken 后签发新 accessToken，并**轮换 refreshToken**（旧值立即失效、重置 7 天），降低重放风险。
- **报表聚合**：`GROUP BY/SUM` 等 MyBatis Plus QueryWrapper 不支持的聚合改用自定义 SQL；门店与全局报表共用聚合逻辑，靠 `storeId IS NULL` 分支切换范围。

---

## 八、技术亮点

1. **路由放行与业务鉴权分离**：过滤器只负责解析身份，角色 / 店铺归属校验集中在 Service 层 `AuthUtil`（`requireRole` / `requireStoreAccess`），职责清晰、便于单测。
2. **精确的安全放行**：商品浏览类 GET 用单段通配符 `*` 精确放行，避免误开同前缀下的受保护接口（如 `/api/stores/*/orders`、`/reports`）。
3. **无状态 + 刷新令牌轮换**：JWT 无状态鉴权配合 Redis refreshToken 轮换，兼顾水平扩展与安全。
4. **统一响应与全局异常**：`Result<T>` + `@RestControllerAdvice` 一处收口，错误码语义化，前端处理一致。
5. **健壮的测试体系**：199 个纯 Mockito 单元测试，并沉淀了 MyBatis Plus 3.5.9 在测试中的多处坑位与解法（见 [debug记录.md](./debug记录.md)），如 `BaseMapper` 重载消歧、`ServiceImpl.baseMapper` 反射注入、Lambda 缓存缺失等。
6. **文档驱动开发**：需求 / 接口 / 实现 / 决策 / 调试 / 前端对接六类文档齐全，技术决策均有「为什么」留痕（[重要决策说明.md](./重要决策说明.md) 41 条）。
7. **数据一致性细节**：下单整链事务化、快照防篡改、取消/退款自动恢复库存与优惠券。

---

## 九、快速开始

### 环境依赖

- JDK 17、Maven
- MySQL 8.x（默认 `localhost:3305`，库名 `bluewhale`，账号 `root/123456`）
- Redis 6.x+（默认 `localhost:6379`）

> 连接参数见 [application.yml](../src/main/resources/application.yml)，支持 `REDIS_HOST` / `JWT_SECRET` 等环境变量覆盖。

### 启动步骤

```bash
# 1. 初始化数据库（只需建空库，建表与种子由 Flyway 启动时自动迁移）
#    CREATE DATABASE bluewhale DEFAULT CHARSET utf8mb4;

# 2. 编译
mvn clean package -DskipTests

# 3. 运行
mvn spring-boot:run
```

启动成功后访问 `http://localhost:8080`。游客接口示例：`GET http://localhost:8080/api/products`。

> 测试账号创建（CUSTOMER 直接注册，STAFF 需 Admin 建店绑定，ADMIN 需 SQL 改角色）详见 [前端对接文档 §6.4](./frontend-handoff.md)。

---

## 十、测试

```bash
mvn test
```

- 199 个单元测试，覆盖各 Service 的正常流程、鉴权、边界与异常场景。
- 采用纯 Mockito（不依赖数据库/Spring 上下文），运行快。
- 事务行为需集成测试验证，当前以单元测试覆盖业务逻辑为主（见 [debug记录.md](./debug记录.md) 第 4 条）。

---

## 十一、项目结构

```
src/main/java/com/twentyzhang/bluewhale/
├── BluewhaleApplication.java
├── controller/      # 15 个 REST 控制器
├── service/         # 11 个业务接口
│   └── impl/        # 11 个实现
├── entity/          # 12 个数据库实体
├── mapper/          # 12 个 MyBatis Plus Mapper
├── dto/             # 55 个请求/响应 DTO
├── common/          # Result<T>、AuthUser
├── config/          # Security / MyBatisPlus / Redis 配置
├── filter/          # JwtAuthenticationFilter
├── util/            # JwtUtil、RedisUtil、AuthUtil
└── exception/       # BusinessException、GlobalExceptionHandler

docs/                # 需求 / 接口 / 实现 / 决策 / 调试 / 前端对接 / 建表脚本
```

---

## 十二、文档导航

| 文档 | 内容 |
|---|---|
| [上手指南.md](./上手指南.md) | 新会话快速了解项目的阅读顺序与协作规范 |
| [requirements.md](./requirements.md) | 需求规格 |
| [api-spec.md](./api-spec.md) | 完整接口规范 |
| [frontend-handoff.md](./frontend-handoff.md) | 前端对接文档（鉴权 / 错误码 / 接口清单 / 枚举 / 启动） |
| [实现说明.md](./实现说明.md) | 各模块「怎么做」的业务逻辑细节 |
| [重要决策说明.md](./重要决策说明.md) | 技术决策的「为什么」 |
| [debug记录.md](./debug记录.md) | 已踩的坑与解法（编写测试前必读） |
| [进度.md](./进度.md) | 开发进度 |
| [数据库迁移指南.md](./数据库迁移指南.md) | Flyway 迁移机制与日常操作 |
| [V1__init_schema.sql](../src/main/resources/db/migration/V1__init_schema.sql) | 数据库建表脚本（Flyway 迁移） |

---

## 十三、暂未实现与后续规划

当前为 MVP 阶段，以下功能有意未纳入开发范围（详见 [requirements.md §5](./requirements.md)）：

- 推荐算法、实时客服（WebSocket）
- 未支付订单定时自动取消（Spring Scheduler）
- 第三方支付对接（当前为模拟支付）
- 优惠券超发的分布式锁防护、多券叠加
- 报表文件导出（Excel/PDF，当前返回 JSON）

已知可优化项（见 [进度.md](./进度.md) 已知技术债）：

- 未登录访问受保护接口返回 403 而非语义化的 401。
- 非法请求体（JSON 解析失败）落入 500 而非 400。
- 库存扣减暂无乐观锁，高并发存在超卖风险。

---

> 本项目为软件工程课程 Lab 实践，业务逻辑与测试体系完整，可作为 Spring Boot 3 + MyBatis Plus 分层后端的参考实现。
