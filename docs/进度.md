# 南鲸商城 — 开发进度

> 最后更新：2026-06-27（AI-2 RAG 导购问答实现完成：SSE 流式 + qwen 生成 + 复用 AI-1 检索，全量测试 326 通过 0 跳过；AI 两子项目全部收口）

---

## 总览

| 层级                     | 状态                          |
| ------------------------ | ----------------------------- |
| Entity / Mapper / DTO    | 全部完成                      |
| Service 接口             | 全部完成                      |
| Service 实现（业务逻辑） | 全部完成                      |
| Controller 路由定义      | 全部完成                      |
| JWT 认证                 | 完成（Filter 已接入，含刷新接口） |
| 公共基础设施             | 完成                          |

---

## 基础设施

| 组件                                 | 状态         | 备注                                                             |
| ------------------------------------ | ------------ | ---------------------------------------------------------------- |
| `common/Result<T>`                 | 完成         | 统一响应封装，替代旧 `dto/ApiResponse<T>`                      |
| `exception/BusinessException`      | 完成         | 默认 code 400                                                    |
| `exception/GlobalExceptionHandler` | 完成         | 处理 Validation / Business / Runtime 三类异常                    |
| `config/RedisConfig`               | 完成         | StringRedisTemplate + StringRedisSerializer                      |
| `util/RedisUtil`                   | 完成         | set / setWithExpire / get / delete / hasKey / expire / getExpire |
| `config/MybatisPlusConfig`         | 完成         | 分页插件                                                         |
| `config/SecurityConfig`            | 完成 | Stateless，JWT Filter 已接入；`/api/auth/**` 与商品浏览类 GET 接口开放，其余需认证（详见决策第 40 条） |
| Flyway 数据库迁移                   | 完成 | `db/migration/V1__init_schema.sql`（建表）+ `R__seed_data.sql`（种子），启动自动执行；详见 `数据库迁移指南.md` 与决策第 43 条 |

**待做：**

- [x] 引入 JWT 依赖（`jjwt` 0.12.6，含 api / impl / jackson）
- [x] 实现 JWT 工具类（`util/JwtUtil.java`，含 generateToken / getUserId / getRole / getStoreId / isValid）
- [x] 实现 `JwtAuthenticationFilter`（`filter/JwtAuthenticationFilter.java`），解析 Bearer Token 写入 SecurityContext
- [x] 新增 `common/AuthUser.java` record，作为 SecurityContext principal，含 userId / role / storeId
- [x] `SecurityConfig` 注入 Filter，注册在 `UsernamePasswordAuthenticationFilter` 之前
- [x] Controller 中占位方法统一替换为 `currentUser()` 辅助方法，从 SecurityContext 读取 `AuthUser`
- [x] 全面切换 Controller 返回值为 `Result<T>`，旧 `dto/ApiResponse.java` 已删除

---

## 业务模块实现进度

### 模块一：用户（UserService）

| 方法               | 接口                           | 状态   |
| ------------------ | ------------------------------ | ------ |
| `register`       | POST `/api/auth/register`    | 完成 |
| `login`          | POST `/api/auth/login`       | 完成 |
| `refreshToken`   | POST `/api/auth/refresh`     | 完成 |
| `getUserInfo`    | GET `/api/users/me`          | 完成 |
| `updateProfile`  | PUT `/api/users/me`          | 完成 |
| `changePassword` | PUT `/api/users/me/password` | 完成 |

---

### 模块二：商店（StoreService）

| 方法                      | 接口                          | 状态   |
| ------------------------- | ----------------------------- | ------ |
| `listStores`            | GET `/api/stores`           | 完成 |
| `getStoreDetail`        | GET `/api/stores/{storeId}` | 完成 |
| `createStore`           | POST `/api/stores`          | 完成 |
| `updateStore`           | PUT `/api/stores/{storeId}` | 完成 |
| `listAllStoresForAdmin` | GET `/api/admin/stores`     | 完成 |

---

### 模块三：商品分类（ProductCategoryService）

| 方法                | 接口                                    | 状态   |
| ------------------- | --------------------------------------- | ------ |
| `getCategoryTree` | GET `/api/categories`                 | 完成 |
| `createCategory`  | POST `/api/categories`                | 完成 |
| `deleteCategory`  | DELETE `/api/categories/{categoryId}` | 完成 |

---

### 模块四：商品（ProductService）

| 方法                  | 接口                                                     | 状态   |
| --------------------- | -------------------------------------------------------- | ------ |
| `listStoreProducts` | GET `/api/stores/{storeId}/products`                   | 完成 |
| `getProductDetail`  | GET `/api/products/{productId}`                        | 完成 |
| `searchProducts`    | GET `/api/products`                                    | 完成 |
| `createProduct`     | POST `/api/stores/{storeId}/products`                  | 完成 |
| `updateProduct`     | PUT `/api/stores/{storeId}/products/{productId}`       | 完成 |
| `deleteProduct`     | DELETE `/api/stores/{storeId}/products/{productId}`    | 完成 |
| `updateStock`       | PUT `/api/stores/{storeId}/products/{productId}/stock` | 完成 |

---

### 模块五：购物车（CartService）

| 方法           | 接口                                | 状态   |
| -------------- | ----------------------------------- | ------ |
| `getCart`    | GET `/api/cart`                   | 完成 |
| `addItem`    | POST `/api/cart/items`            | 完成 |
| `updateItem` | PUT `/api/cart/items/{itemId}`    | 完成 |
| `removeItem` | DELETE `/api/cart/items/{itemId}` | 完成 |

---

### 模块六：订单（OrderService）

| 方法                | 接口                                                 | 状态   |
| ------------------- | ---------------------------------------------------- | ------ |
| `createOrder`     | POST `/api/orders`（支持多券叠加 `couponIds`）       | 完成 |
| `previewCoupon`   | POST `/api/orders/coupon-preview`（下单前优惠试算）  | 完成 |
| `getMyOrders`     | GET `/api/orders`                                  | 完成 |
| `getOrderDetail`  | GET `/api/orders/{orderId}`                        | 完成 |
| `payOrder`        | POST `/api/orders/{orderId}/pay`                   | 完成 |
| `cancelOrder`     | POST `/api/orders/{orderId}/cancel`                | 完成 |
| `confirmReceived` | POST `/api/orders/{orderId}/confirm`               | 完成 |
| `refundOrder`     | POST `/api/orders/{orderId}/refund`                | 完成 |
| `getStoreOrders`  | GET `/api/stores/{storeId}/orders`                 | 完成 |
| `shipOrder`       | POST `/api/stores/{storeId}/orders/{orderId}/ship` | 完成 |
| `cancelExpiredUnpaidOrder` | 定时任务（无接口，`OrderAutocancelJob` 每分钟扫描） | 完成 |

---

### 模块七：收货地址（AddressService）

| 方法              | 接口                                  | 状态   |
| ----------------- | ------------------------------------- | ------ |
| `listAddresses` | GET `/api/addresses`                | 完成 |
| `createAddress` | POST `/api/addresses`               | 完成 |
| `updateAddress` | PUT `/api/addresses/{addressId}`    | 完成 |
| `deleteAddress` | DELETE `/api/addresses/{addressId}` | 完成 |

---

### 模块八：评论（ReviewService）

| 方法             | 接口                                       | 状态   |
| ---------------- | ------------------------------------------ | ------ |
| `listReviews`  | GET `/api/products/{productId}/reviews`  | 完成 |
| `createReview` | POST `/api/products/{productId}/reviews` | 完成 |
| `createReply`  | POST `/api/reviews/{reviewId}/replies`   | 完成 |

---

### 模块九：优惠券组（CouponGroupService / CouponService）

| 方法                        | 接口                                         | 状态   |
| --------------------------- | -------------------------------------------- | ------ |
| `listCouponGroups`        | GET `/api/coupon-groups`                   | 完成 |
| `createStoreCouponGroup`  | POST `/api/stores/{storeId}/coupon-groups` | 完成 |
| `createGlobalCouponGroup` | POST `/api/coupon-groups`                  | 完成 |
| `deleteCouponGroup`       | DELETE `/api/coupon-groups/{groupId}`      | 完成 |
| `claimCoupon`             | POST `/api/coupon-groups/{groupId}/claim`  | 完成 |
| `getMyCoupons`            | GET `/api/coupons/mine`                    | 完成 |

---

### 模块十：报表（ReportService）

| 方法                     | 接口                                         | 状态   |
| ------------------------ | -------------------------------------------- | ------ |
| `getStoreOrderReport`  | GET `/api/stores/{storeId}/reports/orders` | 完成 |
| `getGlobalOrderReport` | GET `/api/admin/reports/orders`            | 完成 |
| 门店报表导出 xlsx        | GET `/api/stores/{storeId}/reports/orders/export` | 完成 |
| 全局报表导出 xlsx        | GET `/api/admin/reports/orders/export`     | 完成 |

---

---

### 模块十一：实时客服（ChatService）

> 路线图任务 7。STOMP over WebSocket 消息路由 + 会话认领制 + Redis 在线状态。

| 方法 / 事件 | 接口 / 目的地 | 状态 |
| --- | --- | --- |
| 会话列表（角色感知） | GET `/api/chat/sessions` | 完成 |
| 历史消息游标分页 | GET `/api/chat/sessions/{id}/messages` | 完成 |
| 接入会话 | POST `/api/chat/sessions/{id}/claim` | 完成 |
| 释放会话 | POST `/api/chat/sessions/{id}/release` | 完成 |
| 买家发消息（WS） | STOMP `/app/chat.customer.send` | 完成 |
| 客服发消息（WS） | STOMP `/app/chat.staff.send` | 完成 |
| 店铺主题订阅（WS） | STOMP `/topic/store.{storeId}` | 完成 |
| 私信投递（WS） | STOMP `/user/queue/messages` | 完成 |
| WS 错误回执 | STOMP `/user/queue/errors` | 完成 |

**关键能力：**
- STOMP over WebSocket（`/ws` 端点），JWT 在 CONNECT 帧鉴权（`StompAuthChannelInterceptor`）
- 会话认领制：`assignee_staff_id` 原子 UPDATE 实现「接入/释放」，防两客服撞单
- 在线状态：Redis Set（买家 `cs:online:customers`、客服 `cs:online:store:{storeId}`），`WebSocketPresenceListener` 实时维护
- 按归属路由：未接入→广播店铺主题；已接入→`convertAndSendToUser` 点对点投递
- 离线粘性：掉线期间 assignee 保持，重连后拉历史补齐
- 数据迁移：`V4__chat.sql`（`chat_session` + `chat_message` 两张表）
- **第二轮 B 收尾**：接待会话超时自动释放（`ChatSessionTimeoutJob` 每 5 分钟扫描失联客服）+ STOMP 结构化 ERROR 帧（`ChatStompErrorHandler`）+ SimpleBroker 心跳兜底在线状态脏数据；任务 7 三项遗留已全部解决

**测试（全部通过，合计 260 个）：**
- `ChatServiceTest`（21 个，纯 Mockito）
- `StompAuthChannelInterceptorTest`（8 个，纯 Mockito）
- `WebSocketPresenceListenerTest`（5 个，纯 Mockito）
- `RedisUtil` 相关（+3 个）
- `ChatWebSocketIntegrationTest`（端到端集成，`@SpringBootTest` + `WebSocketStompClient`，全链路 + 鉴权拒绝，需 MySQL + Redis）

---

### 模块十二：推荐（RecommendationService）

> 路线图任务 8。item-based 协同过滤（加权余弦），离线预计算 + MySQL 持久 + Redis ZSET 缓存。

| 方法 / 任务 | 接口 / 目的 | 状态 |
| --- | --- | --- |
| `getRelated` | GET `/api/products/{productId}/recommendations`（开放） | 完成 |
| `getPersonalized` | GET `/api/recommendations`（需登录） | 完成 |
| `rebuildAll` | POST `/api/admin/recommendations/rebuild`（Admin 手动） | 完成 |
| 定时重建 | `RecommendationRebuildJob` 每日 03:00 cron | 完成 |

**关键能力：**
- 加权余弦相似度，共现口径=同一用户跨订单；状态加权 `PAID/SHIPPED/COMPLETED`=1.0、`PENDING_PAYMENT`=0.5、`CANCELLED`=0.3，同用户同商品取最大。
- 多级缓存：`product_similarity` 表为真相来源，Redis ZSET（`rec:sim:{id}`）作热读缓存，read-through 未命中回表并回填。
- 冷启动兜底：CF 不足 → 同类目热销 → 全站热销（按 `SUM(quantity)` 按需 SQL 聚合）。
- 个性化聚合排除已购；Admin 鉴权在 Controller 层（`rebuildAll` 也被无 SecurityContext 的定时任务调用）。
- 数据迁移：`V5__recommendation.sql`。
- **第二轮 C 增强**：空相似结果哨兵（`rec:sim:empty:{id}` 防冷门商品反复回表）+ 评分加权（`review.rating` 经 `ratingMultiplier` 乘入重建权重）；C2 增量重建暂缓。

**测试（全部通过，新增 14 个）：**
- `CosineSimilarityTest`（6 个，纯函数，含手算例子断言）
- `RecommendationServiceTest`（6 个，纯 Mockito：重建 / A 缓存命中·未命中·兜底 / B 新用户·聚合排除已购）
- `RecommendationControllerTest`（2 个，`@WebMvcTest`）

---

### 模块十三：支付（PaymentService）

> 第二轮任务 D。可插拔模拟支付网关，复刻第三方支付异步回路：发起建流水 → 模拟收银台触发延迟异步回调 → HMAC 验签 + 幂等推进订单状态。

| 方法 / 端点 | 接口 | 状态 |
| --- | --- | --- |
| `initiatePayment` | POST `/api/orders/{orderId}/pay`（**破坏性变更**：返回 `{tradeNo,payUrl}` 而非直接 PAID） | 完成 |
| `handleNotify` | POST `/api/payments/notify`（webhook，开放，HMAC 验签） | 完成 |
| `queryPayment` | GET `/api/payments/{tradeNo}`（查单，需登录，前端轮询） | 完成 |
| `triggerMockCallback` | POST `/api/mock-pay/{tradeNo}/success`、`/fail`（模拟收银台，开放） | 完成 |

**关键能力：**
- 网关抽象 `PaymentGateway`（`MockPaymentGateway` 实现，HMAC-SHA256 验签），未来可平滑替换为 `AlipayPaymentGateway`。
- 真异步：收银台立即返回 → `TaskScheduler`（`paymentCallbackScheduler`）延迟 ~1.5s 发回调 → 前端轮询查单（真支付宝标准姿势）。
- 幂等 + 并发安全：流水状态守卫 `advanceStatus`（`WHERE status='PENDING'` 原子推进）+ 订单 `markPaid`（`WHERE status='PENDING_PAYMENT'` 原子置 PAID），重复/并发回调不重复改单；验签失败、金额不符、未知交易被拒。
- 回调路径（`handleNotify`/`triggerMockCallback`）由异步线程与 HTTP 两条路径调用，**不读 SecurityContext**；鉴权（CUSTOMER + 订单归属）放在 REST 路径 `initiatePayment`/`queryPayment`。
- 订单 1:N 流水：`payment` 表，`trade_no` 唯一幂等键；迁移 `V6__payment.sql`。
- 移除旧同步 `OrderService.payOrder` 与 `dto/PayOrderResponse`。

**测试：**
- `PaymentSignUtilTest`（4 个，纯函数 HMAC 签名/验签）
- `PaymentServiceTest`（10 个，纯 Mockito：发起/回调成功·验签失败·幂等·金额不符·失败回调/触发调度）
- `PaymentFlowIntegrationTest`（端到端：发起→收银台→轮询查单 SUCCESS/FAILED + 订单状态推进，需 MySQL + Redis；基础设施不可用时优雅跳过）

---

### 模块十四：AI 语义搜索（SemanticSearchService）

> AI-1。独立于关键词搜索的语义检索链路：transactional outbox 同步「商品变更 → 向量索引」+ Qdrant 向量库 + 通义 `text-embedding-v3`（1024 维）。

| 方法 / 任务 | 接口 / 目的 | 状态 |
| --- | --- | --- |
| `search` | GET `/api/products/semantic`（开放，失败降级关键词搜索） | 完成 |
| `reindexAll` | POST `/api/admin/products/reindex`（Admin 全量回填） | 完成 |
| 商品写入队 outbox | `ProductServiceImpl` create/update→UPSERT、delete→DELETE（同事务） | 完成 |
| 中继同步 | `OutboxRelayJob` 每 5s 轮询 PENDING → embed → 同步 Qdrant | 完成 |

**关键能力：**
- **方案 C transactional outbox**：商品写与 `index_outbox` 事件同事务原子提交；中继异步消费，AI 基础设施不可用不影响商品写（事件留 PENDING 重试）。
- **幂等 + 至少一次投递**：Qdrant 以 `productId` 作 point id，upsert/delete 天然幂等；失败重试 ≤ maxRetry(5) 超限置 FAILED；Qdrant 不可用整批跳过下轮重试。
- **过滤下推**：分类/价格作 Qdrant payload 过滤在向量库侧筛；命中按 score 重排回表。
- **优雅降级**：embedding/Qdrant 失败回退 `ProductService.searchProducts` 关键词搜索。
- **零新增 Maven 依赖**：Spring `RestClient` 直调 Qdrant/通义 REST。
- 数据迁移：`V7__index_outbox.sql`。embedding 文本 = 商品名 + 类目名（`product` 无描述字段，留作后续加固点）。

**测试（全部通过）：**
- `SemanticSearchServiceTest`（5 个，纯 Mockito：相关性重排 / 过滤下推 / embedding·Qdrant 失败降级 / reindex 入队）
- `OutboxRelayJobTest`（6 个，纯 Mockito：UPSERT·DELETE 分派 / 缺失商品退化 delete / 重试·超限 FAILED / Qdrant 不可用整批跳过）
- `ProductServiceTest` 新增 2 个（create 入队 UPSERT / delete 入队 DELETE）
- `SemanticSearchIntegrationTest`（端到端，`@SpringBootTest` + 桩 embedding + 真实 Qdrant；Qdrant 不可达时 `assumeTrue` 优雅跳过）

**真实联调验证（2026-06-27）：** 用阿里百炼 workspace key（公网 DashScope 端点即可跑 `text-embedding-v3`/1024 维，无需改 URL）对 14 个种子商品 reindex 后实测：「做饭用的调味料」→ 橄榄油/酱油，「夏天解渴的饮料」→ 气泡水/黑咖啡，「送朋友的健康礼物」→ 坚果/饼干（查询词均无字面匹配，纯语义召回）。详见 `实现说明.md`「本地联调与验证」。测试方法见同节。

---

### 模块十五：AI 导购问答（RagService，RAG）

> AI-2。在 AI-1 检索基座上叠加 LLM 生成：单轮、SSE 流式的商品导购问答。检索复用 `SemanticSearchService`，生成用通义千问 `qwen-plus`（DashScope OpenAI 兼容，`stream=true`）。

| 方法 / 组件 | 接口 / 目的 | 状态 |
| --- | --- | --- |
| `RagService.answer` | GET `/api/products/qa`（开放，SSE 流式） | 完成 |
| `QwenChatClient` | qwen 流式对话（`RestClient.exchange` 读流 + 解析 OpenAI SSE chunk） | 完成 |
| `RagPromptBuilder` | 构造 system（严格 grounding）+ user（候选商品上下文）messages | 完成 |
| `ragStreamExecutor` | 后台流式线程池，SseEmitter 主线程秒返回不阻塞 | 完成 |

**关键能力：**
- **检索增强生成闭环**：检索复用 AI-1（一份 `ProductListItemResponse` 既当商品卡又当 LLM 上下文）→ 构造 prompt → qwen 流式生成。
- **SSE 事件协议**：`products`（先推商品卡）→ `answer`（流式 token，多次）→ `done`；异常推 `error`。
- **严格 grounding + 空结果短路**：system 约束「只能从候选商品推荐、不得编造」；检索为空不调 qwen，短路返回提示语。
- **可插拔生成**：`ChatCompletionClient` 抽象（OpenAI 兼容），切 DeepSeek 仅改 `base-url`/`model`/`key` 配置。
- **零新增依赖、无数据库迁移**：`RestClient` + `SseEmitter`；qwen 与 embedding 共用 `DASHSCOPE_API_KEY`。

**测试（全部通过，新增 9 个）：**
- `QwenChatClientTest`（3 个：extractDelta 取 content / 缺失·非法返回 null / consumeStream 逐行取 delta 且 `[DONE]` 终止）
- `RagPromptBuilderTest`（2 个：system+user 两条含 grounding 约束 / 候选数受 context-size 限制）
- `RagServiceTest`（4 个，纯 Mockito + 同步 executor：正常事件序列 products→answer*→done / 空结果短路不调 qwen / 生成异常推 error / 检索异常推 error）

---

## 建议实现顺序

```
1. JWT 基础设施（依赖 → 工具类 → Filter → SecurityConfig 接入）
2. 用户模块（注册 + 登录，产出可用 Token，其余模块都需要鉴权）
3. 商品浏览（分类树、商店列表、商品列表/详情/搜索，无需登录，便于早期联调）
4. 购物车模块
5. 收货地址模块
6. 商店管理 + 商品管理（Staff/Admin 操作）
7. 订单模块（依赖购物车、地址、商品库存）
8. 优惠券模块（领取 + 下单时核销，依赖订单模块）
9. 评论模块（依赖订单完成状态）
10. 报表模块（依赖订单数据）
```

---

## 已知技术债

| 项目 | 状态 | 说明 |
| ---- | ---- | ---- |
| `ApiResponse<T>` 残留 | 已清除 | Controller 已全面切换到 `Result<T>`，旧类已删除 |
| `getCurrentUserId()` 返回 null | 已解决 | JWT Filter 接入后统一从 SecurityContext（`AuthUtil`）读取 |
| JWT 依赖缺失 | 已解决 | `jjwt` 0.12.6（api/impl/jackson）已引入 |
| 未登录返回 403 而非 401 | 已解决 | 新增 `RestAuthenticationEntryPoint`（未认证 → 401）+ `RestAccessDeniedHandler`（无权限 → 403），`SecurityConfig.exceptionHandling` 注册（第二轮 A1） |
| 恶意请求体返回 500 | 已解决 | `GlobalExceptionHandler` 增 `HttpMessageNotReadableException` 处理 → 业务码 400（第二轮 A2） |
| `restoreInventory` 读-改-写 | 已解决 | 取消/退款恢复库存改用原子 `ProductMapper.increaseStock`（`stock = stock + q`），消除丢失更新窗口（第二轮 A3） |
| 库存超卖 | 已解决 | `createOrder` 扣减、`updateStock` 调整均改为乐观锁（`@Version` + 自定义 SQL `stock >= quantity` 原子条件 + 重试 3 次），见决策第 45 条、迁移 `V2__add_product_version.sql` |
| 优惠券超发 | 已解决 | `claimCoupon` 加 Redis 分布式锁（组级互斥）+ 双重检查 + 扣减守卫 `remain_count > 0`，见决策第 46 条、`util/RedisLockUtil` |

---

## 下一阶段规划

全部业务模块已完成，后续按 **[下一阶段路线图.md](./下一阶段路线图.md)** 分三阶段补齐 `requirements.md §5` 的暂缓功能：

- **阶段一（优先，已全部完成）**：~~任务 1 未支付订单自动取消（定时器）✅~~ → ~~任务 2 并发安全（库存超卖 ✅ + 优惠券超发 ✅）~~ → ~~任务 3 热点读 Redis 缓存 ✅~~（`CacheUtil`/`CacheKeys` + 6 类缓存点 + 失效，见决策第 47 条）
- **阶段二（已全部完成）**：~~任务 4 报表导出 Excel ✅~~（EasyExcel 多 Sheet，决策 #48）→ ~~任务 5 多优惠券叠加策略 ✅~~（券类型拆 3 类 + 多券最优顺序计价 + 试算接口，决策 #49）
- **阶段三（按需）**：~~任务 6 支付宝沙箱~~（⛔ 不做，由第二轮任务 D 模拟支付网关替代）、~~任务 7 实时客服（WebSocket）✅~~、~~任务 8 推荐算法（item-based CF）✅~~

> 各任务的方案、关键文件、依赖与文档义务见路线图；完成后回到本表与路线图同步状态。

---

## AI 应用方向（新方向，规划中）

> 在功能已收口的基础上，新增 AI 应用场景作品展示。两个子项目共享「embedding + 向量库」基座，各自独立 spec → plan → 实现循环。

| 子项目 | 状态 | 设计 / 计划文档 |
| --- | --- | --- |
| ① AI 语义商品搜索 | **完成**（见上「模块十四」） | spec：`docs/superpowers/specs/2026-06-15-ai语义搜索-design.md`；plan：`docs/superpowers/plans/2026-06-15-ai语义搜索.md`（12 任务） |
| ② RAG 导购问答 | **完成**（见上「模块十五」） | spec：`docs/superpowers/specs/2026-06-27-rag-design.md`；plan：`docs/superpowers/plans/2026-06-27-rag导购问答.md`（6 任务） |

**子项目①已定技术选型（详见 spec / plan）：**
- 方案 C：transactional outbox 可靠同步「商品变更 → 向量索引」（核心学习点）。
- 向量库 Qdrant（Docker 单容器）；embedding 用通义 `text-embedding-v3`（1024 维）；生成侧 DeepSeek 留给②。
- 新增独立接口 `GET /api/products/semantic`（语义检索 + 分类/价格过滤下推），失败降级现有关键词搜索；Admin `POST /api/admin/products/reindex` 全量回填。
- 用 Spring `RestClient` 调 Qdrant/通义，**零新增 Maven 依赖**；迁移 `V7__index_outbox.sql`。

**实现前置**：本地起 Qdrant（`docker run -d -p 6333:6333 qdrant/qdrant`）；联调真实 embedding 需通义 DashScope API key（单测与「Qdrant 在跑」的集成测试用桩，不需 key）。

> ✅ AI-1 已落地：本表①已置「完成」、新增「模块十四：AI 语义搜索」详表，并已同步 `实现说明.md` / `重要决策说明.md`（#57）/ `api-spec.md` / `frontend-handoff.md` / `数据库迁移指南.md`（V7）/ `下一阶段路线图.md`。下一步轮到子项目② RAG（待 brainstorming → spec → plan）。
